text_sentiment = TASK: Analyze the customer reviews for the product "{{products_name}}" and return ONLY a JSON object, no prose, no code fences.
CONSTRAINTS:
  - Use ONLY the provided reviews. No speculation. No usernames or personal data.
  - Weight reviews annotated with more "[helpful: X yes / Y no]" votes more heavily.
  - Respond in English.
JSON SHAPE (return exactly this shape):
{"dominant_sentiment":"positive|negative|neutral|mixed","strengths":["..."],"issues":["..."],"themes":[{"label":"...","frequency":0,"sentiment":"positive|neutral|negative"}],"quotes":{"positive":["..."],"negative":["..."]},"summary":"3-5 sentence paragraph"}
=== CUSTOMER REVIEWS ===
{{text_reviews}}



text_fidelity_check = TASK: You are a strict fact-checker. The CUSTOMER REVIEWS below are the ONLY ground truth. For the ANALYSIS CLAIMS, return ONLY a JSON object, no prose, no code fences.
CONSTRAINTS:
  - A claim is "supported" only if the reviews state it or clearly imply it.
  - List every claim that is NOT supported by the reviews.
  - Respond in English.
JSON SHAPE (return exactly this shape):
{"supported_fraction":0.0,"unsupported_claims":["..."]}
=== CUSTOMER REVIEWS ===
{{text_reviews}}
=== ANALYSIS CLAIMS ===
{{claims}}



text_translate_analysis = TASK: Translate ONLY the human-readable string values of the JSON below into {{language}}. Return ONLY the JSON object, exactly the same shape and keys, no prose, no code fences.
CONSTRAINTS:
  - Do NOT change field names, the "dominant_sentiment" value, any theme "sentiment" value, or the "frequency" numbers.
  - Keep the exact same structure and array lengths.
JSON:
{{analysis_json}}
