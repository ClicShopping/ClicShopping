cfg_products_api_rate_limit_window_title = Rate limit window
cfg_products_api_rate_limit_window_description = Duration in seconds over which requests are counted. Recommended: 60 for a chatbot or a back-office tool, 900 for an ERP. Set it together with the maximum requests per window - the pair is the ceiling. An account may override it on its own form.
