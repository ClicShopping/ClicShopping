llm_prompt_order_by_correction = You are an SQL expert. Fix the ORDER BY column reference error in this query.

Error: {{error_message}}
Unknown Column: {{unknown_column}}

```sql
{{query}}
```

When using aggregate functions (YEAR, QUARTER, MONTH, etc.) in GROUP BY:

1. Use the SAME function expression in ORDER BY:
   ✅ CORRECT: GROUP BY YEAR(date) ORDER BY YEAR(date)
   ❌ WRONG: GROUP BY YEAR(date) ORDER BY year

2. OR use column position numbers:
   ✅ CORRECT: SELECT YEAR(date), SUM(value) ... ORDER BY 1

3. OR use the alias if you created one:
   ✅ CORRECT: SELECT YEAR(date) AS year_value ... ORDER BY year_value

Fix ONLY the ORDER BY clause. Preserve the SELECT and GROUP BY clauses exactly as they are.

Return ONLY the corrected SQL query, nothing else.

llm_prompt_reasoning_correction = You are an expert SQL debugging assistant. Analyze and fix this SQL error using step-by-step reasoning.

--- ERROR CONTEXT ---
Error Type: {{error_type}}
Error Message: {{error_message}}
Failed Query:
```sql
{{failed_query}}
```
{{original_query_block}}
{{similar_cases_block}}
--- YOUR TASK ---
Analyze this error step by step:
1. **Understand**: What is the root cause of this error?
2. **Plan**: What changes are needed to fix it?
3. **Apply**: Generate the corrected SQL query
4. **Validate**: Check if the correction makes sense

Respond in this format:
REASONING: <your step-by-step analysis>
CORRECTED_QUERY: <the fixed SQL query>
CONFIDENCE: <0.0 to 1.0>
SUGGESTIONS: <optional improvement suggestions>

llm_prompt_reasoning_original_query = Original User Question: {{original_query}}

llm_prompt_reasoning_similar_cases_header = --- SIMILAR CASES FROM HISTORY ---

llm_prompt_reasoning_similar_case = Case {{index}}:
- Original Error: {{original_error}}
- Correction Applied: {{correction_method}}
- Similarity: {{similarity}}%
