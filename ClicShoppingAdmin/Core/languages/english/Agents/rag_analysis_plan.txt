text_analysis_plan_prompt = TASK: produce an ANALYSIS PLAN for the question below. Return ONLY valid JSON, no text before or after, no markdown fence.
{{metric_catalog}}
PLAN FORMAT
{"periods": {"current": {"from": "YYYY-MM-DD", "to": "YYYY-MM-DD"}, "compare": "none|previous_year|previous_period"}, "metrics": [{"name": "<a name from the list above>"}], "unsupported": ["<a measure the question asks for that the list does not carry>"], "dimensions": ["<what the result is broken down by>"], "rankings": [{"metric": "<a metric you listed>", "direction": "top|bottom", "n": 3, "partition_by": "<dimension or null>"}], "filters": {}, "complexity": 1}
RULES
1. metrics: use ONLY names from the list above - never invent a name, never approximate with a neighbour.
1bis. unsupported: when the question asks for a measure the list does not carry, keep it OUT of metrics and name it there, in the words of the question. It is how the answer tells the user what it could not serve; an omission it cannot name is an amputation. Leave it empty when the question asks for no measure at all - a stock level, a price, a list - that is not an unsupported measure.
1ter. metrics: a question that asks for RECORDS is not a metric aggregation - "the last N orders", "list the customers", one product's price, a stock level. Return metrics EMPTY for it, and the question is handled without a plan. Proposing a metric there costs twice: it replaces the records with a count, and it forces a window the question never asked for - and the plan is BINDING, so that invented window becomes law and the answer comes back empty.
2. periods.current: the window the question actually asks about. Never widen it, never narrow it. It is the window the question CARRIES, never a default and never today: a question that carries none is one you must not have proposed a metric for (rule 1ter).
3. compare: say WHETHER to compare, never WHAT to compare with. The second window is computed for you. Writing it yourself is how an empty interval gets produced.
4. Do NOT write the grain or the type of a metric: both are already known and will be filled in.
5. rankings: one entry per ranking. A top 3 and a bottom 3 are TWO entries. partition_by names the dimension inside which the ranking applies.
6. complexity: 1 to 5, for the audit trail only. It routes nothing.
{{examples}}
QUESTION
{{question}}
RETURN THE JSON NOW.
text_analysis_plan_header = ANALYSIS PLAN - this plan is BINDING. Do not add a metric it does not list, do not change a grain, do not change a window.
text_analysis_plan_metric_line = - {{name}} | grain: {{grain}} | type: {{type}} | {{variation}}
text_analysis_plan_variation_percent = variation is (current - previous) / previous * 100
text_analysis_plan_variation_points = variation is current - previous, in PERCENTAGE POINTS, never a ratio of two rates
text_analysis_plan_window_current = current window: {{from}} to {{to}}
text_analysis_plan_window_previous = previous window: {{from}} to {{to}} - same length as the current one, use it as given
text_analysis_plan_time_grain = time grain: DAY, on a DENSE axis. Build the elapsed-day axis from seq_1_to_{{days}} - the built-in MariaDB table of the integers 1 to N, whose single column is named seq - and LEFT JOIN each window's per-day aggregate onto it, so a day with no activity contributes 0 instead of vanishing from the series. Carry a running total over that axis for each window separately, with SUM(...) OVER (ORDER BY the elapsed day). Each window keeps its own column: never infer which window a row belongs to from its elapsed day, both windows share the same elapsed days. Never return one aggregate per window: the cumulative curve, the month-to-date and the daily drill-down cannot be rebuilt from two totals afterwards.
text_analysis_plan_time_grain_shape_daily = presentation: return ONE ROW PER ELAPSED DAY, from day 1 to day {{days}}, ordered by elapsed day. Every row carries the elapsed day, the calendar date of each window as current_day and previous_day - never as current_date, a reserved word - the running total of each window and their variation; the last row IS the reading to date.
text_analysis_plan_time_grain_shape_monthly = presentation: the current window spans {{days}} days, so keep ONLY the rows whose current-window calendar date is the last day of its month, plus elapsed day {{days}} - never one row per day. Carry the running totals over the FULL dense axis inside a CTE, then elect those rows in an OUTER query over that CTE: a WHERE sitting in the same SELECT as the window function is applied BEFORE it and zeroes every cumulative. Every row carries the elapsed day, the calendar date of each window as current_day and previous_day - never as current_date, a reserved word - the running total of each window and their variation; the last row IS the reading to date.
text_analysis_plan_ranking_line = - {{direction}} {{n}} by {{metric}}, partitioned by {{partition}}
text_analysis_plan_ranking_no_partition = no partition
text_analysis_plan_dimensions_line = dimensions: {{dimensions}}
