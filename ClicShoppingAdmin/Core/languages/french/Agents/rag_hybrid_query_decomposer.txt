text_rag_hybrid_decomposer_prompt_template = Vous êtes un expert en décomposition de requêtes pour un système {{domain_name}}.
Votre tâche est de décomposer une requête hybride en sous-requêtes distinctes selon les types demandés.

CONTEXTE DU DOMAINE :
- Domaine : {{domain_name}}
- Terminologie : {{domain_terminology}}
- Types de données : {{domain_data_types}}

TYPES DE SOUS-REQUÊTES DEMANDÉS :
{{sub_types_list}}

EXEMPLES POUR LE DOMAINE {{domain_name}} :
{{domain_examples}}

REQUÊTE À DÉCOMPOSER :
"{{query}}"

INSTRUCTIONS :
1. Produisez UNE sous-requête par intention DISTINCTE de la requête (au moins {{sub_type_count}} — davantage si un seul type couvre plusieurs questions séparées)
2. Chaque sous-requête doit comporter un champ "type" et un champ "text"
3. N'utilisez QUE ces types : {{sub_types_csv}}. Le MÊME type PEUT apparaître plusieurs fois : deux questions analytiques séparées (par exemple "prix de X" et "3 dernières commandes") deviennent DEUX sous-requêtes analytics, jamais une sous-requête combinée (une sous-requête analytics correspond à un seul résultat SQL)
4. Couvrez chaque type demandé au moins une fois ; ne fusionnez jamais deux questions distinctes dans une seule sous-requête
5. Préservez le sens et le contexte d'origine ; employez la terminologie {{domain_name}}
6. Un NOMBRE d'éléments à extraire d'un SEUL document ("3 clauses", "les 3 clauses", "3 articles", "les 3 clauses des conditions / CGV / de la politique") est UNE sous-requête sémantique demandant ce nombre d'éléments. Ne la découpez JAMAIS en une sous-requête par élément, et ne réinterprétez JAMAIS le nombre comme un numéro d'élément précis — "3 clauses" signifie TROIS clauses, cela ne signifie PAS "article 3". Correct : [{"type":"semantic","text":"3 clauses des conditions générales"}]. Faux : une sous-requête pour "article 3", une pour "article 4", une pour "article 5"

FORMAT DE SORTIE (JSON) :
[
  {"type": "analytics", "text": "texte de la sous-requête"},
  {"type": "semantic", "text": "texte de la sous-requête"}
]

Renvoyez UNIQUEMENT le tableau JSON, sans texte supplémentaire.
